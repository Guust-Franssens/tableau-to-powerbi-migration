/// Sales fact table for clean unit-check fixture.
table Sales

	/// Order date used to group sales.
	column 'Order Date'
		dataType: dateTime

	/// Total revenue amount for sales.
	measure 'Total Revenue' = SUM(Sales[Amount])
